// Stick in stuff that is continually running.
//You need to view the background page console (access it in the chrome extension part)
var host = window.location.host;
var url = window.location.href;
var hash = window.location.hash;

// if(host == "twitter.com"){

// 	if($("tweet-form").hasClass("tweeting")){
// 		alert("hello world");
// 	}
// }
